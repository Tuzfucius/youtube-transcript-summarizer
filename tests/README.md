# `tests/` 目录说明

这个目录用于保存自动化测试。

## 测试范围

当前测试重点覆盖：

- CLI、Web UI、导出脚本是否正确调用核心接口
- 本地配置读写与敏感信息遮蔽
- 字幕 / 弹幕导出逻辑
- 无网络条件下的模拟总结链路

## 不纳入默认测试的内容

以下内容不放入默认 `pytest`：

- 真实 YouTube 联调
- 真实 Bilibili 联调
- 真实线上 LLM API 调用

这些场景采用手工联调方式验证，避免 CI 或本地测试因为网络波动而失真。

## 运行方式

```bash
pytest
```
