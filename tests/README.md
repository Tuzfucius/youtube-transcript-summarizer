# tests 目录说明

这个目录放测试用例和测试说明。

## 测试目标

- 验证 CLI、Web UI、导出脚本这些入口是否能正确调用核心函数
- 验证本地配置读写和敏感信息遮罩
- 验证字幕/弹幕导出逻辑
- 验证不依赖线上接口的总结链路模拟

## 说明

测试默认不访问真实视频站点，也不依赖线上 API。需要网络的场景应当单独做手工验证，不放进默认测试流程。

## 运行方式

```bash
pytest
```

