# Video Summarizer Requirements
# Mode: full

youtube-transcript-api
requests

# Optional Dependencies (install manually)
# These require more resources but enable advanced features:

# Whisper Transcription (for videos without subtitles)
# pip install faster-whisper
# Note: Requires 4GB+ RAM, CPU supported but GPU recommended

# Video Download (for downloading videos)
# pip install yt-dlp
# Note: Also requires ffmpeg installed on system

# Gradio Web UI (for browser interface)
# pip install gradio
# Note: Requires ~500MB disk space
